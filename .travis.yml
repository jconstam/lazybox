language: cpp
compiler: gcc
dist: xenial

addons:
  apt:
    packages:
      cmake
      python-pip
      lcov

install:
  - pip install --user -r requirements.txt

script:
  - make -j install INSTALL_PATH=$(pwd)/install
  - make -j test