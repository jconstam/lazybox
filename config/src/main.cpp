#include <iostream>
#include <cstdlib>

using namespace std;

int main( int argc, char* argv[] )
{
	system( "clear" );

	cout << "CONFIGURING" << endl;
	cin.ignore( );

	system( "clear" );

	return 0;
}